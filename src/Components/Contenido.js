import React from 'react';
import styles from "@/styles/contenido.module.css"

const Contenido = () => {
  return (
    <div className={styles.container}>Contenido</div>
  )
}

export default Contenido